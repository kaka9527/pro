package com.tin.it.util;

import java.util.concurrent.*;

public class ThreadUtil {
    //public static ExecutorService threadPool = new ThreadPoolExecutor(1,1,0, TimeUnit.MILLISECONDS,new ArrayBlockingQueue<Runnable>(6), Executors.defaultThreadFactory(),new ThreadPoolExecutor.CallerRunsPolicy());
}
